#!/usr/bin/env python

from distutils.core import setup

setup(name      = 'teispice',
	version       = '${PACKAGE_VERSION}',
	description   = 'teispice Circuit Simulator',
	author_email  = 'github@fuesika.de',
	license       = 'GPL2',
	package_dir   = {'': '${CMAKE_CURRENT_SOURCE_DIR}'} )

